# Copy to backend.hcl after running bootstrap (see README.md).
# Values must match bootstrap outputs.

bucket         = "eusphere-tfstate"
key            = "infra/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "eusphere-terraform-locks"
